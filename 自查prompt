1、电梯问题：
请仔细重新审阅我上一条消息中的全部需求，与你当前的实现逐项对照。以下是关键检查清单，请确认每一项是否已正确实现：

功能层面：
□ 6 部电梯是否独立运行，各自有正确的状态管理？
□ 调度算法是否考虑了距离优先、顺路搭载、负载均衡、满载跳过？
□ 时间步进模拟是否正确（0.5秒为步长）？
□ loadScenario() 能否正确加载 JSON 格式的乘客数据？
□ runSimulation() 能否快进跑完全部乘客？
□ getMetrics() 是否返回正确计算的 avgWaitTime/maxWaitTime/avgTravelTime/totalDelivered？
□ getEventLog() 是否记录了每一个事件（乘客到达、电梯移动、上下客）？

可视化层面：
□ 左侧是否有 6 部电梯的实时动画（楼层位置、方向箭头、载客数）？
□ 右侧是否有实时统计面板（已运送/等待中/平均等待/最长等待/各梯状态）？
□ 是否有控制按钮（开始/暂停/重置）和速度切换（1x/5x/10x/20x）？
□ 是否内置了"早高峰"默认场景（硬编码的 100 人固定数据）？
□ 楼层是否从下到上排列（1楼在底部）？
□ 所有乘客是否都能被最终送达？是否没有"丢失"乘客？

请逐项排查，将未实现或有误的部分修复完善。输出修改后的完整 HTML 文件。
